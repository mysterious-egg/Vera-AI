/**
 * composer.service.js — message composition orchestrator.
 *
 * Wires together context, retrieval, and Gemini to produce the final
 * message that Vera sends to a merchant or customer.
 * Implemented in Checkpoint 3 (message composition).
 */

// TODO (Checkpoint 3): implement compose()

export const composerService = {
  /**
   * @param {{ category, merchant, trigger, customer? }} contexts
   * @returns {Promise<string>} composed message body
   */
  // async compose(contexts) { ... }
};
