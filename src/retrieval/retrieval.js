/**
 * retrieval.js — legacy stub, preserved for import-path compatibility.
 *
 * The implementation lives in src/services/retrieval.service.js (CP4).
 * Import from there directly in all new code.
 */

export { retrievalService as retrieval } from '../services/retrieval.service.js';
