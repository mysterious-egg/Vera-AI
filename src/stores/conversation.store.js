/**
 * conversation.store.js — in-memory store for active conversations.
 *
 * Tracks every conversation the bot has initiated, keyed by conversation_id.
 * Implemented in Checkpoint 4 (conversation management).
 */

// TODO (Checkpoint 4): implement get / set / appendTurn

export const conversationStore = {};
