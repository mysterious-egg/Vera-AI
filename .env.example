# Environment variables — copy to .env and fill in values
# Never commit the real .env file

PORT=3000
NODE_ENV=development

# Gemini (to be configured in a later checkpoint)
# GEMINI_API_KEY=your_key_here
